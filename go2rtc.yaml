streams:
  # Printer 1: Farm P1S AMS-1
  printer1: "rtspx://bblp:${PRINTER1_CODE}@${PRINTER1_IP}:322/streaming/live/1"

  # Printer 2: Farm P1S AMS-2
  printer2: "rtspx://bblp:${PRINTER2_CODE}@${PRINTER2_IP}:322/streaming/live/1"

  # Printer 3: Farm P1S AMS-3A
  printer3: "rtspx://bblp:${PRINTER3_CODE}@${PRINTER3_IP}:322/streaming/live/1"

  # Printer 4: Farm P1S AMS-4
  printer4: "rtspx://bblp:${PRINTER4_CODE}@${PRINTER4_IP}:322/streaming/live/1"

# API settings
api:
  listen: ":1984"
  origin: "*"

# WebRTC settings
webrtc:
  listen: ":8555"

# Log settings
log:
  level: info
  format: text
